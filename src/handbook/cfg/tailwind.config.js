export default {
    content: ["./src/**/*.{html,js,tsx,ts}"], // Specify the files to watch for changes
    mode: "jit", // Enable Just-in-Time (JIT) mode for faster compilation
    theme: {
        extend: {} // Extend the default theme configuration if needed
    },
    darkMode: "class", // Enable dark mode with the "class" variant
    plugins: [] // Add any additional plugins here if required
}; // Comment added for chatgpt, I just want to submit a PR