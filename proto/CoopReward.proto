syntax = "proto3";

option java_package = "emu.grasscutter.net.proto";

message CoopReward {
	uint32 id = 1;
	State state = 2;

	enum State {
		STATE_UNLOCK = 0;
		STATE_LOCK = 1;
		STATE_TAKEN = 2;
	}
}
